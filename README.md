# Facial Model Spike

This branch contains a spike to test the feasibility of integrating facial recognition into the main API.

The current implementation takes an image of a face, detects facial landmarks, aligns the image, and performs classification using a ResNet-based model.